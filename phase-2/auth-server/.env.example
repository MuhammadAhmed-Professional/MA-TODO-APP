# ==============================================================================
# Environment Variables for Better Auth Server
# ==============================================================================
#
# FOR RAILWAY DEPLOYMENT:
# ------------------------
# Set these variables in the Railway dashboard:
# 1. Go to https://railway.app/project/{your-project-id}
# 2. Click on the "auth-server" service
# 3. Go to the "Variables" tab
# 4. Add the following variables:
#
#    DATABASE_URL: ${{Postgres.DATABASE_URL}}
#      (This references the PostgreSQL database service)
#
#    BETTER_AUTH_URL: https://auth-server-production.up.railway.app
#      (Replace with your actual Railway domain)
#
#    CORS_ORIGINS: http://localhost:3000,https://your-frontend.up.railway.app
#      (Add all allowed origins, comma-separated)
#
#    BETTER_AUTH_SECRET: (already set - see Variables tab)
#
# FOR LOCAL DEVELOPMENT:
# ----------------------
# Copy this file to .env and update the values below
#
# ==============================================================================

# Database Configuration
# Neon Serverless PostgreSQL connection string (MUST MATCH backend/.)
# For Railway: Use ${{Postgres.DATABASE_URL}} as a service reference
DATABASE_URL=postgresql://user:password@ep-example-123456.region.neon.tech/phase2_todo?sslmode=require

# Better Auth Configuration
# For Railway: Use your Railway domain (e.g., https://auth-server-production-8251.up.railway.app)
# For local: http://localhost:3001
BETTER_AUTH_URL=http://localhost:3001

# CRITICAL: Must match backend JWT_SECRET for token compatibility
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-256-bit-secret-key-replace-with-random-value

# CORS Configuration
# Comma-separated list of allowed frontend origins
# For Railway: Include both your frontend Railway domain and localhost for testing
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Server Configuration
PORT=3001

# Environment
NODE_ENV=development

# Email Configuration (Optional - for future email verification)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-smtp-password
# SMTP_FROM=noreply@example.com

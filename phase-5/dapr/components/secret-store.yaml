apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: kubernetes-secrets
  namespace: todo-app
spec:
  type: secretstores.kubernetes
  version: v1
  metadata:
    # No configuration needed - uses in-cluster Kubernetes service account
    # Dapr sidecar will read secrets from the same namespace

    # Optional: Specify default namespace (defaults to component namespace)
    # - name: defaultNamespace
    #   value: "todo-app"

    # Optional: Enable/disable multi-namespace access
    # - name: multipleNamespaces
    #   value: "false"
